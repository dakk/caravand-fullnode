(jbuild_version 1)

(executable
 ((name letchaind)
  (public_name letchaind)
  (modules letchaind)
  (libraries (bitcoinml network utils api blockchain))))

(library
 ((name        letchain)
  (public_name letchain)
  (libraries (cryptokit threads yojson bitcoinml network utils api blockchain))))